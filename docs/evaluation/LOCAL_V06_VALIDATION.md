# 0.6.0.dev0 本地验证

2026-09-15，Windows / Python 3.12.13；未发布，未运行本轮远端CI。数据库head为0006。应用源码与安装包40个应用文件逐字节一致，Ditto上游许可证包含在包内。

## 正确性与升级

- 最终全量：370 passed、0 failed、0 skipped，77.90秒；两条既有Starlette测试客户端弃用提示。包含实际PostgreSQL恢复fixture和离线微型神经训练测试，不能解释为370条均在PostgreSQL执行。
- 另将存储、关系查询、持久任务和工作区/迁移fixture显式切到PostgreSQL，68项通过、0跳过，44.18秒；这是后端补充验证，与全量用例有重叠，不累加成438个独立测试。
- v0.5真实wheel在源码外建立0005库、保存判断和发布身份；新wheel升级0006后来源、ID、判断历史保持。新撤销产生回执，显式发布和旧版本查询正确；升级前DB/产物副本可由旧wheel读取完成回退。没有演示保留升级后写入的降级。
- 两后端均通过真实鉴权HTTP与独立CLI Worker九项工作流检查：幂等导入、访问权限、独立执行、失败回执恢复、传递冲突拒绝、属性更新ID、发布冲突、撤销及旧ID去向、备份恢复一致。
- 回执回归覆盖事件事务、文件写入、rename、绑定提交、并发相同键、旧租约执行者、依据变化、备份篡改和真实进程终止。旧判断历史列表改为按首次事件序号排序。
- Ruff、核心和神经环境pip check通过。Splink/DuckDB/Parquet兼容验证使用新的合成输出目录，80条记录/40配对，模型与TF保存重载后分数不变。

## 容量测量

实际运行已安装的新wheel；服务和Worker的工作目录在源码外。固定双来源合成数据、精确匹配、每桶恰好一对、最大簇2。每格1次预热+3次实测；每次实测30次同版本limit=50目录GET，下表p95使用90次原始样本的nearest-rank统计。全流程时长为提交任务、启动Worker、准备和发布的中位数，导入不计入该时长。

| 后端 | 记录 | 候选对 | 任务至发布中位数(s) | 目录GET p95(ms) | Worker进程树采样RSS峰值(MiB) |
|---|---:|---:|---:|---:|---:|
| SQLite | 100 | 50 | 1.613 | 6.197 | 166.82 |
| SQLite | 1,000 | 500 | 1.901 | 6.847 | 186.65 |
| SQLite | 10,000 | 5,000 | 5.380 | 7.201 | 341.86 |
| PostgreSQL | 100 | 50 | 1.907 | 9.384 | 173.25 |
| PostgreSQL | 1,000 | 500 | 2.251 | 10.309 | 190.16 |
| PostgreSQL | 10,000 | 5,000 | 6.817 | 15.209 | 346.71 |

10k导入HTTP耗时分别为3.250/8.959秒；任务至发布三次范围分别为5.227—5.565/6.649—6.927秒。CPU为AMD64 Family25 Model97、12逻辑核、约31.6GiB RAM。六格串行执行，本机其他工作负载未认证为完全空闲。

内存每10ms采样启动器和后代RSS之和；共享页可能重复计数，不含独立API和数据库服务，不是PSS或整机峰值。修复前脚本只采到Windows虚拟环境启动器约5MB，其旧内存结果作废；修复后用80MiB分配回归并重跑全部六格。未独立测锁等待；队列等待包含进程启动。不从这些点推断并发吞吐、生产SLA、高候选密度、大簇或模型效果。

## 匹配质量与剩余限制

公司复核预算评估输入检查已实现并测试：来源/标签声明、实体与记录互斥、同候选池、未知标签、冻结哈希和预算100/25/50。仍没有提供合格的独立公司人工holdout，草案没有封存，真实公司训练次数为0，新公司质量指标为空；不以合成样例或旧WDC复测代替。

此外，回执默认300秒租约且无续租；列表最多最近200条，无完整分页；未完成第二人验收、真人计时、实际业务接入、生产PITR/RTO或远端跨平台CI。本地可安装和验收通过不等于生产成熟。

复现入口：[工作流与容量操作卡](../../examples/company_workflow/OPERATION_CARD.md)、[回执契约](../decisions/015-review-receipts.md)、[公司评测数据门槛](COMPANY_REVIEW_EVALUATION.md)。原始JUnit、HTTP记录、每轮容量数据及SHA256清单保存在本地本轮验收目录，不随源码公开。
